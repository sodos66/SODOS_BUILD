services:
  # Redis市场数据容器 - 关闭持久化，高性能
  redis_market:
    image: redis:7-alpine
    container_name: redis_market
    restart: unless-stopped
    ports:
      - "6381:6379"
    volumes:
      - ./redis_market/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ./redis_market/data:/data
    command: redis-server /usr/local/etc/redis/redis.conf
    networks:
      - data_network
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "4.0"
        reservations:
          memory: 512M
          cpus: "0.5"
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "SoDosP6zodC9qB2N2Q3b1slZp7Pdx4", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Redis缓存容器 - 开启完整持久化
  redis_cache:
    image: redis:7-alpine
    container_name: redis_cache
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - ./redis_cache/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ./redis_cache/data:/data
    command: redis-server /usr/local/etc/redis/redis.conf
    networks:
      - data_network
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "4.0"
        reservations:
          memory: 512M
          cpus: "0.5"
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "SoDosP6zodC9qB2N2Q3b1slZp7Pdx4", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Elasticsearch K线数据存储容器
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.12.2
    container_name: elasticsearch_kline
    restart: unless-stopped
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      - ELASTIC_PASSWORD=SoDosP6zodC9qB2N2Q3b1slZp7Pdx4
      - ELASTIC_USERNAME=elastic
      - "ES_JAVA_OPTS=-Xms2g -Xmx4g"
      - bootstrap.memory_lock=true
      - discovery.type=single-node
      - xpack.security.enabled=true
      - xpack.security.http.ssl.enabled=false
      - xpack.security.transport.ssl.enabled=false
    volumes:
      - ./elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - ./elasticsearch/config/jvm.options:/usr/share/elasticsearch/config/jvm.options:ro
      - ./elasticsearch/data:/usr/share/elasticsearch/data
      - ./elasticsearch/logs:/usr/share/elasticsearch/logs
    networks:
      - data_network
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
        reservations:
          memory: 1G
          cpus: "0.5"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -u elastic:SoDosP6zodC9qB2N2Q3b1slZp7Pdx4 -f http://localhost:9200/_cluster/health || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # ElasticVue - Elasticsearch管理界面
  elasticvue:
    image: cars10/elasticvue:latest
    container_name: elasticvue
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./elasticvue/config/config.json:/usr/share/nginx/html/api/config.json:ro
    environment:
      - ELASTICVUE_CLUSTERS=[{"name":"kline-cluster","uri":"http://elasticsearch:9200","username":"elastic","password":"SoDosP6zodC9qB2N2Q3b1slZp7Pdx4"}]
    networks:
      - data_network
    depends_on:
      - elasticsearch
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
        reservations:
          memory: 128M
          cpus: "0.1"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # LibreTranslate - 翻译服务
  libretranslate:
    image: libretranslate/libretranslate:latest
    container_name: libretranslate
    restart: unless-stopped
    ports:
      - "9000:5000"
    environment:
      - LT_DISABLE_WEB_UI=false
      - LT_DISABLE_FILES_TRANSLATION=false
    networks:
      - data_network
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2.0"
        reservations:
          memory: 512M
          cpus: "0.5"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: mysql_sodos
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=zOffP6yF6v4VIbaPoNn0veoMOumhzBCQCvoafV36qZ9s3xqZaBn3E7IGDR5KKaDg
      - MYSQL_DATABASE=sodos_exchange
      - MYSQL_USER=sodos_exchange
      - MYSQL_PASSWORD=SoDosP6zodC9qB2N2Q3b1slZp7Pdx4
    volumes:
      - ./mysql/config/production.cnf:/etc/mysql/conf.d/production.cnf:ro
      - ./mysql/data:/var/lib/mysql
      - ./mysql/logs:/var/log/mysql
    networks:
      - data_network
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "4.0"
        reservations:
          memory: 1G
          cpus: "1"
    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "-uroot",
          "-p zOffP6yF6v4VIbaPoNn0veoMOumhzBCQCvoafV36qZ9s3xqZaBn3E7IGDR5KKaDg",
          "ping",
          "-h",
          "127.0.0.1",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # SODOS Exchange Run Server
  sodos_runserver:
    image: sodos66/exchange_run_server:prod
    container_name: sodos_runserver
    restart: unless-stopped
    ports:
      - "9501:9501"
      - "9510:9510"
      - "9511:9511"
    volumes:
      - ./app/SODOS_EXCHANGE:/SODOS_EXCHANGE
    working_dir: /SODOS_EXCHANGE
    command:
      - /bin/sh
      - -c
      - |
        if [ ! -f /SODOS_EXCHANGE/.env ] && [ -f /SODOS_EXCHANGE/prod.env ]; then
          cp /SODOS_EXCHANGE/prod.env /SODOS_EXCHANGE/.env;
        fi;
        composer install --ignore-platform-reqs && php bin/hyperf.php start;
        tail -f /dev/null
    depends_on:
      - mysql
      - redis_cache
      - redis_market
      - elasticsearch
    networks:
      - data_network
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "6.0"
        reservations:
          memory: 512M
          cpus: "0.5"

  # 管理后台前端
  sodos_manager:
    image: sodos66/sodos_admin:prod
    container_name: sodos_manager
    restart: unless-stopped
    ports:
      - "9520:80"
    volumes:
      - ./logs/manager:/var/log/nginx
    networks:
      - data_network

# 创建数据服务专用网络桥
networks:
  data_network:
    name: data_network
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

# 数据卷定义
volumes:
  redis_market_data:
    driver: local
  redis_cache_data:
    driver: local
  elasticsearch_data:
    driver: local
  elasticsearch_logs:
    driver: local
